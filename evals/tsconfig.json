{
  "extends": "../tsconfig.base.json",
  // The harness drives the E2E test kit (build-excluded TypeScript source in
  // services/buyer), so it is run from source by tsx and only typechecked here.
  "compilerOptions": { "noEmit": true },
  "include": ["src"],
  "exclude": ["src/**/*.test.ts"]
}
