{
  "name": "@negotiator/evals",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Evals harness: N synthetic negotiations over the in-process stack, honest metrics report",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "evals": "tsx src/cli.ts"
  },
  "dependencies": {
    "@negotiator/ledger": "workspace:*",
    "@negotiator/llm": "workspace:*",
    "@negotiator/protocol": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  }
}
